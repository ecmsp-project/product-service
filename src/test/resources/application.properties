spring.h2.console.enabled=false
spring.jpa.hibernate.ddl-auto=none

spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Kafka configuration for tests
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=product-service-test
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer

# Kafka topics
kafka.topic.order-status-updated=order-status-updated-test

# Disable Kafka auto-start for tests (beans will be created but won't connect)
spring.kafka.consumer.auto-startup=false

# mvn test -Dspring.profiles.active=test

#@ActiveProfiles("test")
#@DataJpaTest
#public class ProductRepositoryTest {
